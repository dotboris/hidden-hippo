before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -y tshark

services: mongodb
language: ruby
rvm:
  - 1.9.3
  - 2.0.0
  - 2.1.5
  - 2.2.0
  - ruby-head
  - jruby-19mode
matrix:
  allow_failures:
    - rvm: ruby-head
    - rvm: jruby-19mode